# Student-Report-Management-System
A lightweight Python-based CRUD application designed to manage student academic records. This system utilizes the csv module for persistent data storage, allowing users to efficiently add, store, and retrieve student details like names, marks, and grades through a structured Command Line Interface (CLI).
